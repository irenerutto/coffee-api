{
  "name": "coffee-api",
  "version": "1.0.0",
  "scripts": {
   "start": "json-server db.json --host 0.0.0.0 --port $PORT --no-static"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}